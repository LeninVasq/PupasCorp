@using PupasCorp.Models.ViewModels
@model Platillo_Menu


<div id="PlatilloMenuModal" class="modal">
    <div class="modal-content">
        <span id="closeModalPlatilloMenu" class="close">&times;</span>

        <form enctype="multipart/form-data" asp-action="Create">
            <div class="form-group">
                <label asp-for="IdMenu" class="control-label">Ingrediente</label>
                <select asp-for="IdMenu" class="form-control" asp-items="ViewBag.Menus">
                </select>
                <span asp-validation-for="IdMenu" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label for="nombre">Cantidad</label>
                <input type="number" asp-for="Cantidad" id="nombre" name="Cantidad" placeholder="Nombre del producto" class="form-control">
            </div>
            <div class="form-actions">
                <button type="button" id="closeModalBtnPlatilloMenu" class="btns btns-secondary">Cerrar</button>
                <button type="submit" class="btns btns-primary">Guardar cambios</button>
            </div>
        </form>

    </div>
</div>