@using PupasCorp.Models.ViewModels
@model Platillo_Menu


<div id="PlatillomenuModalUpdate" class="modal">
    <div class="modal-content">
        <span id="closeModalUpdatePlatillomenu" class="close">&times;</span>

        <form enctype="multipart/form-data" asp-action="update">
            <!--            <div class="form-group">
                <label asp-for="IdIngrediente" class="control-label">Ingrediente</label>
                <select asp-for="IdIngrediente" class="form-control" asp-items="ViewBag.Ingredientes">
                </select>
                <span asp-validation-for="IdIngrediente" class="text-danger"></span>
            </div>
            -->

            <div class="form-group">
                <label for="nombre">Cantidad</label>
                <input type="number" asp-for="Cantidad" id="Cantidadplatillo" name="Cantidad" placeholder="Nombre del producto" class="form-control">
            </div>

            <input type="hidden" asp-for="IdPlatilloMenu" id="IdPlatilloMenu" name="IdPlatilloMenu" placeholder="Nombre del producto" class="form-control">

            <div class="form-actions">
                <button type="button" id="closeModalUpdateBtnPlatillomenu" class="btns btns-secondary">Cerrar</button>
                <button type="submit" class="btns btns-primary">Guardar cambios</button>
            </div>
        </form>

    </div>
</div>